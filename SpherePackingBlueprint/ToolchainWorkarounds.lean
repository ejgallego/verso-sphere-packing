import SpherePackingBlueprint.Macros

set_option autoImplicit false

/--
Temporary placeholders for external `lean := "..."` references from the SpherePacking blueprint port.

XXX Blueprint toolchain workaround: these declarations exist only to keep blueprint links
non-missing while the upstream SpherePacking project is not imported in this workspace toolchain.
-/
-- XXX Blueprint toolchain workaround
axiom D : Prop

-- XXX Blueprint toolchain workaround
axiom Delta : Prop

-- XXX Blueprint toolchain workaround
axiom Delta_imag_axis_pos : Prop

-- XXX Blueprint toolchain workaround
axiom E8Basis_volume : Prop

-- XXX Blueprint toolchain workaround
axiom E8Lattice : Prop

-- XXX Blueprint toolchain workaround
axiom E8Matrix : Prop

-- XXX Blueprint toolchain workaround
axiom E8Packing : Prop

-- XXX Blueprint toolchain workaround
axiom E8Packing_density : Prop

-- XXX Blueprint toolchain workaround
axiom E8_norm_eq_sqrt_even : Prop

-- XXX Blueprint toolchain workaround
axiom E_k_q_expansion : Prop

-- XXX Blueprint toolchain workaround
axiom EisensteinSeries.eisensteinSeries_SIF : Prop

-- XXX Blueprint toolchain workaround
axiom E₂_eq : Prop

-- XXX Blueprint toolchain workaround
axiom E₂_transform : Prop

-- XXX Blueprint toolchain workaround
axiom F : Prop

-- XXX Blueprint toolchain workaround
axiom FG_inequality_1 : Prop

-- XXX Blueprint toolchain workaround
axiom FG_inequality_2 : Prop

-- XXX Blueprint toolchain workaround
axiom F_imag_axis_pos : Prop

-- XXX Blueprint toolchain workaround
axiom FmodG_rightLimitAt_zero : Prop

-- XXX Blueprint toolchain workaround
axiom FmodG_strictAntiOn : Prop

-- XXX Blueprint toolchain workaround
axiom G : Prop

-- XXX Blueprint toolchain workaround
axiom G_imag_axis_pos : Prop

-- XXX Blueprint toolchain workaround
axiom H₂ : Prop

-- XXX Blueprint toolchain workaround
axiom H₂_MF : Prop

-- XXX Blueprint toolchain workaround
axiom H₂_S_action : Prop

-- XXX Blueprint toolchain workaround
axiom H₂_T_action : Prop

-- XXX Blueprint toolchain workaround
axiom H₂_imag_axis_pos : Prop

-- XXX Blueprint toolchain workaround
axiom H₃ : Prop

-- XXX Blueprint toolchain workaround
axiom H₃_MF : Prop

-- XXX Blueprint toolchain workaround
axiom H₃_S_action : Prop

-- XXX Blueprint toolchain workaround
axiom H₃_T_action : Prop

-- XXX Blueprint toolchain workaround
axiom H₄ : Prop

-- XXX Blueprint toolchain workaround
axiom H₄_MF : Prop

-- XXX Blueprint toolchain workaround
axiom H₄_S_action : Prop

-- XXX Blueprint toolchain workaround
axiom H₄_T_action : Prop

-- XXX Blueprint toolchain workaround
axiom H₄_imag_axis_pos : Prop

-- XXX Blueprint toolchain workaround
axiom IsZLattice : Prop

-- XXX Blueprint toolchain workaround
axiom LinearMap.BilinForm.dualSubmodule : Prop

-- XXX Blueprint toolchain workaround
axiom LinearProgrammingBound : Prop

-- XXX Blueprint toolchain workaround
axiom LinearProgrammingBound' : Prop

-- XXX Blueprint toolchain workaround
axiom MLDE_F : Prop

-- XXX Blueprint toolchain workaround
axiom MLDE_G : Prop

-- XXX Blueprint toolchain workaround
axiom MagicFunction.FourierEigenfunctions.a : Prop

-- XXX Blueprint toolchain workaround
axiom MagicFunction.FourierEigenfunctions.b : Prop

-- XXX Blueprint toolchain workaround
axiom MagicFunction.PolyFourierCoeffBound.DivDiscBoundOfPolyFourierCoeff : Prop

-- XXX Blueprint toolchain workaround
axiom MagicFunction.a.Fourier.eig_a : Prop

-- XXX Blueprint toolchain workaround
axiom MagicFunction.a.RadialFunctions.a : Prop

-- XXX Blueprint toolchain workaround
axiom MagicFunction.a.RealIntegrals.a' : Prop

-- XXX Blueprint toolchain workaround
axiom MagicFunction.a.SpecialValues.a_zero : Prop

-- XXX Blueprint toolchain workaround
axiom MagicFunction.b.Fourier.eig_b : Prop

-- XXX Blueprint toolchain workaround
axiom MagicFunction.b.SpecialValues.b_zero : Prop

-- XXX Blueprint toolchain workaround
axiom ModularForm : Prop

-- XXX Blueprint toolchain workaround
axiom ModularForm.eisensteinSeries_MF : Prop

-- XXX Blueprint toolchain workaround
axiom PeriodicSpherePacking : Prop

-- XXX Blueprint toolchain workaround
axiom PeriodicSpherePacking.aux2_ge' : Prop

-- XXX Blueprint toolchain workaround
axiom PeriodicSpherePacking.aux2_le' : Prop

-- XXX Blueprint toolchain workaround
axiom PeriodicSpherePacking.aux_ge : Prop

-- XXX Blueprint toolchain workaround
axiom PeriodicSpherePacking.aux_le : Prop

-- XXX Blueprint toolchain workaround
axiom PeriodicSpherePacking.density_eq : Prop

-- XXX Blueprint toolchain workaround
axiom PeriodicSpherePackingConstant : Prop

-- XXX Blueprint toolchain workaround
axiom Real.fourierIntegral : Prop

-- XXX Blueprint toolchain workaround
axiom SchwartzMap : Prop

-- XXX Blueprint toolchain workaround
axiom SchwartzMap.PoissonSummation_Lattices : Prop

-- XXX Blueprint toolchain workaround
axiom SchwartzMap.fourierTransformCLM : Prop

-- XXX Blueprint toolchain workaround
axiom SpherePacking.MainTheorem : Prop

-- XXX Blueprint toolchain workaround
axiom SpherePacking.balls : Prop

-- XXX Blueprint toolchain workaround
axiom SpherePacking.constant_eq_constant_normalized : Prop

-- XXX Blueprint toolchain workaround
axiom SpherePacking.density : Prop

-- XXX Blueprint toolchain workaround
axiom SpherePacking.finiteDensity : Prop

-- XXX Blueprint toolchain workaround
axiom SpherePacking.finiteDensity_ge : Prop

-- XXX Blueprint toolchain workaround
axiom SpherePacking.finiteDensity_le : Prop

-- XXX Blueprint toolchain workaround
axiom SpherePacking.scale : Prop

-- XXX Blueprint toolchain workaround
axiom SpherePacking.scale_density : Prop

-- XXX Blueprint toolchain workaround
axiom SpherePacking.scale_finiteDensity : Prop

-- XXX Blueprint toolchain workaround
axiom SpherePackingConstant : Prop

-- XXX Blueprint toolchain workaround
axiom Submodule.E8 : Prop

-- XXX Blueprint toolchain workaround
axiom instDiscreteE8Lattice : Prop

-- XXX Blueprint toolchain workaround
axiom instIsZLatticeE8Lattice : Prop

-- XXX Blueprint toolchain workaround
axiom jacobi_identity : Prop

-- XXX Blueprint toolchain workaround
axiom modular_slash_negI_of_even : Prop

-- XXX Blueprint toolchain workaround
axiom periodic_constant_eq_constant : Prop

-- XXX Blueprint toolchain workaround
axiom ramanujan_E₂ : Prop

-- XXX Blueprint toolchain workaround
axiom ramanujan_E₂' : Prop

-- XXX Blueprint toolchain workaround
axiom ramanujan_E₄ : Prop

-- XXX Blueprint toolchain workaround
axiom ramanujan_E₄' : Prop

-- XXX Blueprint toolchain workaround
axiom ramanujan_E₆ : Prop

-- XXX Blueprint toolchain workaround
axiom ramanujan_E₆' : Prop

-- XXX Blueprint toolchain workaround
axiom serre_D : Prop

-- XXX Blueprint toolchain workaround
axiom serre_D_mul : Prop

-- XXX Blueprint toolchain workaround
axiom serre_D_slash_invariant : Prop

-- XXX Blueprint toolchain workaround
axiom span_E8Matrix : Prop

-- XXX Blueprint toolchain workaround
axiom span_E8Matrix_eq_top : Prop

-- XXX Blueprint toolchain workaround
axiom volume_ball_ratio_tendsto_nhds_one'' : Prop

-- XXX Blueprint toolchain workaround
axiom Δ : Prop

-- XXX Blueprint toolchain workaround
axiom Δ_ne_zero : Prop

-- XXX Blueprint toolchain workaround
axiom Θ₂ : Prop

-- XXX Blueprint toolchain workaround
axiom Θ₃ : Prop

-- XXX Blueprint toolchain workaround
axiom Θ₄ : Prop

-- XXX Blueprint toolchain workaround
axiom η : Prop
